#!/bin/sh
HERE="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
if [ -n "${APPIMAGE:-}" ]; then
  export BOOKREADER_APP_DIR="$(CDPATH= cd -- "$(dirname -- "$APPIMAGE")" && pwd)"
fi
exec "$HERE/BookReader/bin/BookReader" "$@"
